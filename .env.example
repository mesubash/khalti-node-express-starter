NODE_ENV=development
PORT=3000

# Use `sandbox` for Khalti test credentials and `production` for real merchant credentials.
KHALTI_ENV=sandbox

# Optional override. Leave empty to use the default base URL for the selected KHALTI_ENV.
KHALTI_BASE_URL=
KHALTI_SECRET_KEY=your_khalti_secret_key

# Local development callback base URL.
# Use the same static ngrok URL pattern as the Spring app.
# If you already have a fixed/free ngrok domain, put it here.
NGROK_URL=https://your-static-domain.ngrok-free.app

# Optional explicit override. In production, set this to your deployed public API base URL.
# PUBLIC_BASE_URL=https://api.yourdomain.com
WEBSITE_URL=http://localhost:5173
CALLBACK_PATH=/api/v1/payments/callback/khalti

# Demo data file used by the starter.
DATA_FILE=./data/runtime.json
